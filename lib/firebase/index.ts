export const TEST_BYPASS_UID = "test-user";

export const TEST_BYPASS_EMAIL = "test@local.dev";

export const TEST_BYPASS_NAME = "Test User";

// Auth-emulator-only credential for the bypass user. Worthless outside the
// local emulator, which is the only place this user exists.
export const TEST_BYPASS_PASSWORD = "test-password";
